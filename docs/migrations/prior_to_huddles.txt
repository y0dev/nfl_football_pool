-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.admins (
  is_active boolean DEFAULT true,
  password_hash text,
  email character varying NOT NULL UNIQUE,
  full_name character varying,
  avatar_url text,
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  created_at timestamp with time zone DEFAULT now(),
  is_super_admin boolean DEFAULT false,
  is_verified boolean DEFAULT false,
  verified_at timestamp with time zone,
  updated_at timestamp with time zone DEFAULT now(),
  plan character varying DEFAULT 'free'::character varying,
  trial_ends_at timestamp with time zone,
  billing_exempt boolean NOT NULL DEFAULT false,
  addon_pools integer NOT NULL DEFAULT 0,
  stripe_customer_id character varying,
  CONSTRAINT admins_pkey PRIMARY KEY (id)
);
CREATE TABLE public.pools (
  require_access_code boolean NOT NULL DEFAULT false,
  monday_night_game_id character varying,
  name character varying NOT NULL,
  logo_url text,
  created_by character varying NOT NULL,
  season integer NOT NULL,
  tie_breaker_method character varying,
  tie_breaker_question character varying,
  tie_breaker_answer integer,
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  created_at timestamp with time zone DEFAULT now(),
  is_active boolean DEFAULT true,
  access_code character varying,
  season_scope ARRAY,
  is_private boolean DEFAULT false,
  join_password text,
  pool_type character varying DEFAULT 'normal'::character varying,
  CONSTRAINT pools_pkey PRIMARY KEY (id),
  CONSTRAINT pools_monday_night_game_id_fkey FOREIGN KEY (monday_night_game_id) REFERENCES public.games(id)
);
CREATE TABLE public.participants (
  pool_id uuid,
  name character varying NOT NULL,
  email character varying,
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  created_at timestamp with time zone DEFAULT now(),
  is_active boolean DEFAULT true,
  CONSTRAINT participants_pkey PRIMARY KEY (id),
  CONSTRAINT participants_pool_id_fkey FOREIGN KEY (pool_id) REFERENCES public.pools(id)
);
CREATE TABLE public.teams (
  name character varying NOT NULL,
  city character varying NOT NULL,
  abbreviation character varying NOT NULL,
  conference character varying,
  division character varying,
  season integer NOT NULL,
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  is_active boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT teams_pkey PRIMARY KEY (id)
);
CREATE TABLE public.games (
  season_type smallint DEFAULT 2,
  id character varying NOT NULL,
  week integer NOT NULL,
  season integer NOT NULL,
  home_team character varying NOT NULL,
  away_team character varying NOT NULL,
  kickoff_time timestamp with time zone NOT NULL,
  home_score integer,
  away_score integer,
  winner character varying,
  home_team_id character varying,
  away_team_id character varying,
  status character varying DEFAULT 'scheduled'::character varying,
  is_playoff boolean DEFAULT false,
  is_active boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  monday_night_score bigint,
  CONSTRAINT games_pkey PRIMARY KEY (id)
);
CREATE TABLE public.picks (
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  participant_id uuid,
  pool_id uuid,
  game_id character varying,
  predicted_winner character varying NOT NULL,
  confidence_points integer NOT NULL,
  submitted_by character varying,
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  locked boolean DEFAULT false,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT picks_pkey PRIMARY KEY (id),
  CONSTRAINT picks_participant_id_fkey FOREIGN KEY (participant_id) REFERENCES public.participants(id),
  CONSTRAINT picks_pool_id_fkey FOREIGN KEY (pool_id) REFERENCES public.pools(id),
  CONSTRAINT picks_game_id_fkey FOREIGN KEY (game_id) REFERENCES public.games(id)
);
CREATE TABLE public.scores (
  participant_id uuid,
  pool_id uuid,
  week integer NOT NULL,
  season integer NOT NULL,
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  points integer DEFAULT 0,
  correct_picks integer DEFAULT 0,
  total_picks integer DEFAULT 0,
  created_at timestamp with time zone DEFAULT now(),
  rank integer,
  is_winner boolean DEFAULT false,
  tie_breaker_used boolean DEFAULT false,
  tie_breaker_rank integer,
  updated_at timestamp with time zone DEFAULT now(),
  season_type integer NOT NULL DEFAULT 2,
  CONSTRAINT scores_pkey PRIMARY KEY (id),
  CONSTRAINT scores_participant_id_fkey FOREIGN KEY (participant_id) REFERENCES public.participants(id),
  CONSTRAINT scores_pool_id_fkey FOREIGN KEY (pool_id) REFERENCES public.pools(id)
);
CREATE TABLE public.tie_breakers (
  participant_id uuid,
  pool_id uuid,
  week integer NOT NULL,
  season integer NOT NULL,
  answer numeric NOT NULL,
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  submitted_at timestamp with time zone DEFAULT now(),
  is_winner boolean DEFAULT false,
  tie_breaker_rank integer,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  rank integer,
  tie_breaker_used boolean DEFAULT false,
  game_id character varying,
  season_type integer DEFAULT 2,
  CONSTRAINT tie_breakers_pkey PRIMARY KEY (id),
  CONSTRAINT tie_breakers_participant_id_fkey FOREIGN KEY (participant_id) REFERENCES public.participants(id),
  CONSTRAINT tie_breakers_pool_id_fkey FOREIGN KEY (pool_id) REFERENCES public.pools(id),
  CONSTRAINT tie_breakers_game_id_fkey FOREIGN KEY (game_id) REFERENCES public.games(id)
);
CREATE TABLE public.audit_logs (
  action character varying NOT NULL,
  admin_id uuid,
  entity character varying NOT NULL,
  entity_id character varying,
  details jsonb,
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT audit_logs_pkey PRIMARY KEY (id),
  CONSTRAINT audit_logs_admin_id_fkey FOREIGN KEY (admin_id) REFERENCES public.admins(id)
);
CREATE TABLE public.admin_verifications (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  created_at timestamp with time zone DEFAULT now(),
  admin_id uuid NOT NULL,
  token character varying NOT NULL UNIQUE,
  expires_at timestamp with time zone NOT NULL,
  used_at timestamp with time zone,
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT admin_verifications_pkey PRIMARY KEY (id),
  CONSTRAINT admin_verifications_admin_id_fkey FOREIGN KEY (admin_id) REFERENCES public.admins(id)
);
CREATE TABLE public.weekly_winners (
  pool_id uuid NOT NULL,
  week integer NOT NULL,
  season integer NOT NULL,
  winner_participant_id uuid,
  winner_name character varying NOT NULL,
  winner_points integer NOT NULL,
  winner_correct_picks integer NOT NULL,
  tie_breaker_question character varying,
  tie_breaker_answer numeric,
  winner_tie_breaker_answer numeric,
  tie_breaker_difference numeric,
  total_participants integer NOT NULL,
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  tie_breaker_used boolean DEFAULT false,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  season_type smallint,
  CONSTRAINT weekly_winners_pkey PRIMARY KEY (id),
  CONSTRAINT weekly_winners_pool_id_fkey FOREIGN KEY (pool_id) REFERENCES public.pools(id),
  CONSTRAINT weekly_winners_winner_participant_id_fkey FOREIGN KEY (winner_participant_id) REFERENCES public.participants(id)
);
CREATE TABLE public.season_winners (
  pool_id uuid NOT NULL,
  season integer NOT NULL,
  winner_participant_id uuid,
  winner_name character varying NOT NULL,
  total_points integer NOT NULL,
  total_correct_picks integer NOT NULL,
  tie_breaker_question character varying,
  tie_breaker_answer numeric,
  winner_tie_breaker_answer numeric,
  tie_breaker_difference numeric,
  total_participants integer NOT NULL,
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  weeks_won integer NOT NULL DEFAULT 0,
  tie_breaker_used boolean DEFAULT false,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT season_winners_pkey PRIMARY KEY (id),
  CONSTRAINT season_winners_pool_id_fkey FOREIGN KEY (pool_id) REFERENCES public.pools(id),
  CONSTRAINT season_winners_winner_participant_id_fkey FOREIGN KEY (winner_participant_id) REFERENCES public.participants(id)
);
CREATE TABLE public.period_winners (
  pool_id uuid NOT NULL,
  season integer NOT NULL,
  period_name character varying NOT NULL,
  start_week integer NOT NULL,
  end_week integer NOT NULL,
  winner_participant_id uuid,
  winner_name character varying NOT NULL,
  period_points integer NOT NULL,
  period_correct_picks integer NOT NULL,
  tie_breaker_question character varying,
  tie_breaker_answer numeric,
  winner_tie_breaker_answer numeric,
  tie_breaker_difference numeric,
  total_participants integer NOT NULL,
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  weeks_won integer NOT NULL DEFAULT 0,
  tie_breaker_used boolean DEFAULT false,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT period_winners_pkey PRIMARY KEY (id),
  CONSTRAINT period_winners_pool_id_fkey FOREIGN KEY (pool_id) REFERENCES public.pools(id),
  CONSTRAINT period_winners_winner_participant_id_fkey FOREIGN KEY (winner_participant_id) REFERENCES public.participants(id)
);
CREATE TABLE public.team_records (
  team_id uuid NOT NULL,
  season integer NOT NULL,
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  wins integer NOT NULL DEFAULT 0,
  losses integer NOT NULL DEFAULT 0,
  ties integer NOT NULL DEFAULT 0,
  home_wins integer NOT NULL DEFAULT 0,
  home_losses integer NOT NULL DEFAULT 0,
  home_ties integer NOT NULL DEFAULT 0,
  road_wins integer NOT NULL DEFAULT 0,
  road_losses integer NOT NULL DEFAULT 0,
  road_ties integer NOT NULL DEFAULT 0,
  points_for integer NOT NULL DEFAULT 0,
  points_against integer NOT NULL DEFAULT 0,
  division_wins integer NOT NULL DEFAULT 0,
  division_losses integer NOT NULL DEFAULT 0,
  division_ties integer NOT NULL DEFAULT 0,
  conference_wins integer NOT NULL DEFAULT 0,
  conference_losses integer NOT NULL DEFAULT 0,
  conference_ties integer NOT NULL DEFAULT 0,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT team_records_pkey PRIMARY KEY (id),
  CONSTRAINT fk_team_record_team FOREIGN KEY (team_id) REFERENCES public.teams(id)
);
CREATE TABLE public.playoff_teams (
  season integer NOT NULL,
  team_name character varying NOT NULL,
  team_abbreviation character varying,
  conference character varying,
  seed integer,
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT playoff_teams_pkey PRIMARY KEY (id)
);
CREATE TABLE public.playoff_confidence_points (
  participant_id uuid NOT NULL,
  pool_id uuid NOT NULL,
  season integer NOT NULL,
  team_name character varying NOT NULL,
  confidence_points integer NOT NULL CHECK (confidence_points > 0),
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT playoff_confidence_points_pkey PRIMARY KEY (id),
  CONSTRAINT playoff_confidence_points_participant_id_fkey FOREIGN KEY (participant_id) REFERENCES public.participants(id),
  CONSTRAINT playoff_confidence_points_pool_id_fkey FOREIGN KEY (pool_id) REFERENCES public.pools(id)
);